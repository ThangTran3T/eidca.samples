<UserControl x:Class="eIDCA.WinApp.Views.Controls.CodePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- REQUEST panel -->
        <Border Grid.Column="0" Style="{StaticResource CardPanel}" Padding="0" Margin="0,0,6,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource BgCard2Brush}" CornerRadius="10,10,0,0"
                        Padding="12,0">
                    <Grid>
                        <TextBlock Text="{Binding RequestTitle}" Style="{StaticResource Caption}"
                                   VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <Button Content="Copy" Style="{StaticResource SecondaryButton}"
                                Command="{Binding CopyRequestCommand}"
                                HorizontalAlignment="Right" FontSize="10" Padding="6,2"/>
                    </Grid>
                </Border>
                <TextBox Grid.Row="1" Text="{Binding RequestJson}" Style="{StaticResource JsonBlock}"
                         MinHeight="120" MaxHeight="300"/>
            </Grid>
        </Border>

        <!-- RESPONSE panel -->
        <Border Grid.Column="1" Style="{StaticResource CardPanel}" Padding="0" Margin="6,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource BgCard2Brush}" CornerRadius="10,10,0,0"
                        Padding="12,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding ResponseTitle}" Style="{StaticResource Caption}"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding StateLabel}" Style="{StaticResource Caption}"/>
                        </StackPanel>
                        <Button Content="Copy" Style="{StaticResource SecondaryButton}"
                                Command="{Binding CopyResponseCommand}"
                                HorizontalAlignment="Right" FontSize="10" Padding="6,2"/>
                    </Grid>
                </Border>
                <TextBox Grid.Row="1" Text="{Binding ResponseJson}" Style="{StaticResource JsonBlock}"
                         MinHeight="120" MaxHeight="300"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
