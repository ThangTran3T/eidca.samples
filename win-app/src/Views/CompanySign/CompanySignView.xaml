<UserControl x:Class="eIDCA.WinApp.Views.CompanySign.CompanySignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:eIDCA.WinApp.Views.Controls"
             xmlns:views="clr-namespace:eIDCA.WinApp.Views.CompanySign"
             xmlns:dl="clr-namespace:eIDCA.WinApp.Views.PersonalSign">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <StackPanel>
                <TextBlock Text="Ký số Tổ chức" Style="{StaticResource H1}"/>
                <TextBlock Text="Upload tài liệu và ký số với CTS gắn theo Tổ chức." Style="{StaticResource Body}" Margin="0,4,0,0"/>
            </StackPanel>
            <controls:StepperControl CurrentStep="{Binding CurrentStep}"
                                     Step0Label="1. Upload tài liệu" Step1Label="2. Ký số" Step2Label="3. Download"/>
            <Border Style="{StaticResource CardPanel}"><views:Step1UploadView DataContext="{Binding Step1}"/></Border>
            <Border Style="{StaticResource CardPanel}"
                    Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=1}">
                <views:Step2SignatureView DataContext="{Binding Step2}"/>
            </Border>
            <Border Style="{StaticResource CardPanel}"
                    Visibility="{Binding CurrentStep, Converter={StaticResource StepVisibilityConverter}, ConverterParameter=2}">
                <!-- Step 3 dùng chung view từ PersonalSign -->
                <dl:Step3DownloadView DataContext="{Binding Step3}"/>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
